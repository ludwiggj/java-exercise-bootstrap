apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'idea'
 
dependencies{
	compile([])
	
	testCompile([
		'junit:junit:4.11',
		'org.jmock:jmock-junit4:2.6.0',
	])
	 
	runtime([])
}

task wrapper(type: Wrapper) { 
	gradleVersion = '2.2.1'
}

